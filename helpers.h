#ifndef HELPERS_H
#define HELPERS_H

// Генерация случайного целого в диапазоне [min, max]
int randomInRange(int min, int max);

// Генерация случайного double в диапазоне [min, max]
double randomDoubleInRange(double min, double max);

// Генерация случайного символа из набора [a-zA-Z0-9]
char randomChar();

// Генерация случайной буквы (латиница)
char randomLetter();

// Генерация случайной цифры
char randomDigit();

#endif
