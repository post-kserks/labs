#ifndef OPTIMAL_H
#define OPTIMAL_H

#include <vector>

// Нахождение максимальной суммы подмассива алгоритмом Кадане
// Возвращает максимальную сумму
int findMaxSubarrayOptimal(const std::vector<int>& nums);

// Вычисляет вычислительную сложность (O(n))
void printOptimalComplexity(int n);

#endif
