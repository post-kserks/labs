# 🧮 Лабораторная работа №7

## Тема: Реализация и сравнение алгоритмов сортировки, поиска и нахождения ближайших точек

### 📌 Цель работы

Реализовать и сравнить по времени работы классические алгоритмы:

1. **Сортировки** — выбором, пузырьком и слиянием;
2. **Поиска** — последовательный и бинарный;
3. **Поиск пары ближайших точек** — методом грубой силы и методом декомпозиции.

Провести вычислительный эксперимент на разных объёмах данных и оценить эффективность каждого алгоритма в лучшем, среднем и худшем случаях.

---

## 🗂 Структура проекта

```
lab7p1/
│
├── Makefile              # Сборка проекта командой make
├── main.cpp              # Главный файл, точка входа
│
├── sort.h / sort.cpp     # Алгоритмы сортировки
├── search.h / search.cpp # Алгоритмы поиска
├── points.h / points.cpp # Поиск ближайших точек
└── utils.h               # Генерация данных и измерение времени
```

---

## ⚙️ Сборка и запуск

```bash
cd lab7p1
make
./lab7p1
```

Результат выполнения выводится в консоль:

```
=== Лабораторная работа №7 ===

Сортировка выбором: 0.25 мс
Пузырьковая сортировка: 1.65 мс
Сортировка слиянием: 0.07 мс

Последовательный поиск: 0.0003 мс
Бинарный поиск: 0.00003 мс

Ближайшие точки (brute): 1.09 мс
Ближайшие точки (divide): 0.41 мс
```

---

## 🧠 Логика программы

### 1. `utils.h` — вспомогательные функции

```cpp
inline std::vector<int> generateRandomArray(int n, int maxVal = 1000);
template<typename Func> double measureTime(Func f);
```

* **`generateRandomArray`** создаёт вектор случайных чисел длиной `n`.

  * Используется для тестов сортировки и поиска.
* **`measureTime`** принимает функцию `f`, выполняет её и возвращает время в миллисекундах.

  * Использует `std::chrono` для высокоточного измерения.

🧩 *Пример:*

```cpp
double t = measureTime([&](){ selectionSort(arr); });
```

---

### 2. `sort.cpp / sort.h` — алгоритмы сортировки

#### 🔹 Сортировка выбором (Selection Sort)

```cpp
void selectionSort(std::vector<int>& arr);
```

**Принцип работы:**

1. На каждой итерации ищется минимальный элемент в неотсортированной части массива.
2. Этот элемент меняется местами с текущим.
3. Повторяется до конца массива.

**Сложность:**

* Время: `O(n²)`
* Память: `O(1)`
* Особенность: одинаковое время для всех случаев (лучший = худший).

---

#### 🔹 Пузырьковая сортировка (Bubble Sort)

```cpp
void bubbleSort(std::vector<int>& arr);
```

**Принцип работы:**

1. Проходим по массиву, сравнивая соседние элементы.
2. Если левый больше правого — меняем местами.
3. После каждого прохода “всплывает” максимальный элемент.
4. Если за проход не было обменов — массив уже отсортирован (оптимизация).

**Сложность:**

* Лучший случай (отсортированный): `O(n)`
* Средний/худший: `O(n²)`
* Память: `O(1)`

---

#### 🔹 Сортировка слиянием (Merge Sort)

```cpp
void mergeSort(std::vector<int>& arr);
```

**Принцип работы (метод декомпозиции):**

1. Массив делится пополам рекурсивно, пока не останется по 1 элементу.
2. Затем пары отсортированных частей сливаются в один массив с помощью функции `merge`.

**Сложность:**

* Время: `O(n log n)`
* Память: `O(n)`
* Стабильная сортировка.

---

### 3. `search.cpp / search.h` — алгоритмы поиска

#### 🔹 Последовательный поиск (Linear Search)

```cpp
int linearSearch(const std::vector<int>& arr, int target);
```

**Принцип работы:**

1. Перебирает элементы массива по порядку.
2. Если найден элемент `target` — возвращает его индекс.
3. Если не найден — возвращает `-1`.

**Сложность:**

* Лучший случай: `O(1)`
* Средний: `O(n/2)`
* Худший: `O(n)`

---

#### 🔹 Бинарный поиск (Binary Search)

```cpp
int binarySearch(const std::vector<int>& arr, int target);
```

**Принцип работы:**

1. Работает только с отсортированным массивом.
2. Делит массив пополам и сравнивает средний элемент с `target`.
3. Если `target` меньше — ищет в левой половине, иначе в правой.
4. Повторяет до нахождения или до исчерпания диапазона.

**Сложность:**

* Все случаи: `O(log n)`
* Память: `O(1)`

---

### 4. `points.cpp / points.h` — поиск пары ближайших точек

#### 🔹 Метод грубой силы (Brute Force)

```cpp
double bruteForceClosest(const std::vector<Point>& points);
```

**Принцип работы:**

1. Сравнивает каждую пару точек `(i, j)`.
2. Вычисляет расстояние по формуле:
   [
   d = \sqrt{(x_i - x_j)^2 + (y_i - y_j)^2}
   ]
3. Возвращает минимальное расстояние.

**Сложность:**

* Время: `O(n²)`
* Простая, но медленная реализация.

---

#### 🔹 Метод декомпозиции (Divide and Conquer)

```cpp
double divideAndConquerClosest(std::vector<Point> points);
```

**Принцип работы:**

1. Сортирует точки по координате `x`.
2. Делит их на две половины.
3. Рекурсивно ищет минимальные расстояния в левой и правой частях.
4. Рассматривает точки в «полосе» шириной `2*d` вокруг разделяющей линии, где `d` — минимум из левой и правой частей.
5. Возвращает минимальное найденное расстояние.

**Сложность:**

* Время: `O(n log n)`
* Память: `O(n)`
* Быстрее грубой силы при больших `n`.

---

### 5. `main.cpp` — выполнение экспериментов

```cpp
int main() {
    // Генерация массива
    std::vector<int> arr = generateRandomArray(1000);

    // Копии массива для разных сортировок
    auto arr1 = arr, arr2 = arr, arr3 = arr;

    // Замер времени выполнения каждой сортировки
    double t1 = measureTime([&](){ selectionSort(arr1); });
    double t2 = measureTime([&](){ bubbleSort(arr2); });
    double t3 = measureTime([&](){ mergeSort(arr3); });

    // Замер времени поиска
    int target = arr3[arr3.size() / 2];
    double t4 = measureTime([&](){ linearSearch(arr3, target); });
    double t5 = measureTime([&](){ binarySearch(arr3, target); });

    // Замер времени поиска ближайших точек
    std::vector<Point> pts(1000);
    for (auto& p : pts) { p.x = rand() % 1000; p.y = rand() % 1000; }
    double tb1 = measureTime([&](){ bruteForceClosest(pts); });
    double tb2 = measureTime([&](){ divideAndConquerClosest(pts); });

    // Вывод результатов
    ...
}
```

Программа замеряет время работы каждого алгоритма и выводит результаты в миллисекундах.

---

## 📈 Теоретическая оценка эффективности

| Алгоритм                    | Сложность  | Характер                    |
| --------------------------- | ---------- | --------------------------- |
| Selection Sort              | O(n²)      | Всегда одинаковое время     |
| Bubble Sort                 | O(n²)      | Лучший O(n), худший O(n²)   |
| Merge Sort                  | O(n log n) | Быстрый, стабильный         |
| Linear Search               | O(n)       | Прост, но медленный         |
| Binary Search               | O(log n)   | Быстрый, требует сортировки |
| Brute Force (points)        | O(n²)      | Простая реализация          |
| Divide and Conquer (points) | O(n log n) | Быстрее на больших входах   |

---

## 🧩 Пример результата (n = 1000)

| Алгоритм                 | Время (мс) |
| ------------------------ | ---------- |
| Сортировка выбором       | 0.25       |
| Пузырьковая сортировка   | 1.65       |
| Сортировка слиянием      | 0.07       |
| Последовательный поиск   | 0.0003     |
| Бинарный поиск           | 0.00003    |
| Ближайшие точки (brute)  | 1.09       |
| Ближайшие точки (divide) | 0.41       |

Результаты подтверждают теоретическую оценку:

* **Merge Sort** работает быстрее квадратичных сортировок;
* **Binary Search** эффективнее линейного;
* **Divide and Conquer** быстрее brute-force при поиске ближайших точек.

---

## 🏁 Выводы

1. Сложность алгоритмов напрямую влияет на время выполнения даже при умеренных размерах данных.
2. Для больших массивов предпочтительнее **Merge Sort** и **Binary Search**.
3. В задаче поиска ближайших точек метод декомпозиции даёт выигрыш в производительности при `n > 1000`.
4. Структура проекта позволяет легко добавлять новые алгоритмы и повторять эксперименты.
