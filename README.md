# Лабораторная работа: Социальная сеть - анализ связей

Кейс 3. Анализ социальных связей с использованием алгоритмов теории графов.

## 📋 Описание задачи

Реализация системы анализа социальной сети, включающая:

1. **Построение графа связей** - транзитивное замыкание, определение степени знакомства
2. **Анализ влияния** - центральность пользователей, поиск мостов, кластеризация
3. **Моделирование распространения информации** - скорость охвата, инфлюенсеры

## 🏗️ Структура проекта

```
social_network/
├── main.cpp       # Главный файл программы
├── graph.h        # Базовые функции работы с графом
├── warshall.h     # Часть 1: Алгоритм Уоршелла
├── floyd.h        # Алгоритм Флойда-Уоршелла
├── analysis.h     # Часть 2: Анализ влияния
├── spread.h       # Часть 3: Распространение информации
├── Makefile       # Система сборки
└── README.md      # Эта документация
```

## 🚀 Запуск проекта

### Сборка и запуск

```bash
cd social_network
make
./social_network
```

### Альтернативные команды

```bash
make run          # Собрать и сразу запустить
make clean        # Удалить скомпилированные файлы
make rebuild      # Пересобрать проект заново
```

## 📚 Теоретическая часть

### Часть 1: Алгоритм Уоршелла

**Алгоритм Уоршелла** используется для построения транзитивного замыкания графа.

**Принцип работы:**
- Если существует путь из вершины i в k и из k в j, то добавляется связь i → j
- Проверяется для всех троек вершин (i, k, j)

**Применение:**
- Определение всех косвенных связей в социальной сети
- Ответ на вопрос "может ли пользователь A достучаться до пользователя B?"

**Код:**
```cpp
for (int k = 0; k < n; k++) {
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            graph[i][j] = graph[i][j] || (graph[i][k] && graph[k][j]);
        }
    }
}
```

**Степень знакомства** определяется с помощью BFS (поиск в ширину) - это количество рукопожатий между двумя пользователями.

### Часть 2: Алгоритм Флойда-Уоршелла

**Алгоритм Флойда-Уоршелла** находит кратчайшие пути между всеми парами вершин.

**Принцип работы:**
- Для каждой промежуточной вершины k пытаемся улучшить путь между i и j
- Если путь через k короче текущего, обновляем расстояние

**Код:**
```cpp
for (int k = 0; k < n; k++) {
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            if (dist[i][k] + dist[k][j] < dist[i][j]) {
                dist[i][j] = dist[i][k] + dist[k][j];
            }
        }
    }
}
```

**Применения:**

1. **Центральность (Closeness Centrality)** - насколько пользователь близок ко всем остальным
   - Формула: `centrality = reachable_users / sum_of_distances`
   - Чем выше значение, тем более влиятельный пользователь

2. **Мосты** - связи, удаление которых разрывает связность графа
   - Критичные отношения в социальной сети
   - Удаление моста изолирует часть пользователей

3. **Кластеризация** - группировка близких пользователей
   - Простой метод: пользователи с расстоянием ≤ 2 входят в один кластер
   - Показывает сообщества в социальной сети

4. **Рекомендации друзей** - система "друзья друзей"
   - Находим общих друзей с незнакомыми пользователями
   - Чем больше общих друзей, тем выше рекомендация

### Часть 3: Моделирование распространения

**Волновой алгоритм** для симуляции распространения постов:

1. Начальный пользователь публикует пост (шаг 0)
2. На каждом шаге пост видят все друзья тех, кто уже видел пост
3. Процесс продолжается до полного охвата или пока есть новые пользователи

**Метрики:**
- **Охват** - процент пользователей, увидевших пост
- **Скорость** - количество новых пользователей на каждом шаге
- **Инфлюенсеры** - пользователи с максимальным потенциальным охватом

**Определение инфлюенсеров:**
- Запускаем симуляцию от каждого пользователя
- Считаем, скольких человек они могут охватить
- Сортируем по убыванию охвата

## 💡 Особенности реализации

- **Без ООП** - только функции и массивы (по требованию)
- **Простота** - минимальный объем кода, понятная структура
- **Комментарии на русском** - для лучшего понимания
- **Модульность** - каждая часть в отдельном заголовочном файле
- **Наглядность** - вывод промежуточных результатов в консоль

## 📊 Пример вывода

Программа выводит:
- Исходную матрицу смежности
- Транзитивное замыкание
- Визуализацию графа
- Примеры степени знакомства
- Матрицу кратчайших расстояний
- Центральность каждого пользователя
- Найденные мосты
- Кластеры
- Рекомендации друзей
- Симуляцию распространения поста
- Топ инфлюенсеров

## 🎓 Авторы

Лабораторная работа выполнена в рамках курса по алгоритмам и структурам данных.

## 📝 Примечания

- Размер сети: 10 пользователей (можно изменить в `main.cpp`)
- Максимальное количество: 15 пользователей (константа `MAX_USERS`)
- Граф создается вручную для демонстрации работы алгоритмов
- Можно легко адаптировать для загрузки данных из файла
