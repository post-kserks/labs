# Лабораторная работа №4. Численные методы решения нелинейных уравнений

## 📋 Оглавление
1. [Описание проекта](#описание-проекта)
2. [Структура проекта](#структура-проекта)
3. [Требования к системе](#требования-к-системе)
4. [Инструкция по сборке и запуску](#инструкция-по-сборке-и-запуску)
5. [Математическая постановка задачи](#математическая-постановка-задачи)
6. [Подробное описание методов](#подробное-описание-методов)
7. [Объяснение работы кода](#объяснение-работы-кода)
8. [Анализ результатов](#анализ-результатов)
9. [Возможные проблемы и решения](#возможные-проблемы-и-решения)

## 🎯 Описание проекта

Данный проект реализует три численных метода для решения нелинейного уравнения:
**x² - eˣ + 2 = 0**

Цель работы - сравнительный анализ эффективности методов:
- Метода половинного деления (бисекции)
- Метода простых итераций
- Метода Ньютона (касательных)

Проект демонстрирует, как различные алгоритмы приближаются к корню уравнения с заданной точностью и сравнивает их по количеству итераций и скорости сходимости.

## 📁 Структура проекта

```
lab4/
├── include/                 # Заголовочные файлы
│   ├── functions.h         # Математические функции
│   └── methods.h           # Структуры и объявления методов
├── src/                    # Исходный код
│   ├── main.cpp            # Главная программа и вывод результатов
│   ├── bisection.cpp       # Реализация метода половинного деления
│   ├── iteration.cpp       # Реализация метода простых итераций
│   └── newton.cpp          # Реализация метода Ньютона
├── Makefile                # Файл для сборки проекта
└── README.md              # Эта документация
```

## 💻 Требования к системе

- **Компилятор**: GCC 4.8+ или Clang 3.5+
- **Операционная система**: Linux, macOS, Windows (с WSL или MinGW)
- **Память**: не менее 256 МБ ОЗУ
- **Дисковое пространство**: не менее 10 МБ

## 🚀 Инструкция по сборке и запуску

### Шаг 1: Клонирование проекта
```bash
git clone <repository-url>
cd lab4
```

### Шаг 2: Сборка проекта
```bash
make
```
Эта команда:
- Создает папку `obj/` для объектных файлов
- Компилирует все исходные файлы
- Линкует их в исполняемый файл `lab4`

### Шаг 3: Запуск программы
```bash
./lab4
```

### Шаг 4: Очистка (опционально)
```bash
make clean
```
Удаляет скомпилированные файлы и исполняемый файл.

## 📐 Математическая постановка задачи

### Исходное уравнение
```
f(x) = x² - eˣ + 2 = 0
```

### Интервал изоляции корня
После анализа функции был найден интервал, содержащий корень:
```
[1.0, 1.5]
```
Проверка:
- f(1.0) = 1² - e¹ + 2 ≈ 0.2817 > 0
- f(1.5) = 1.5² - e¹·⁵ + 2 ≈ -0.2317 < 0
- f(1.0) × f(1.5) < 0 → корень существует

### Требуемые точности
1. ε = 10⁻⁶ = 0.000001
2. ε = 10⁻¹¹ = 0.00000000001

## 🔍 Подробное описание методов

### 1. Метод половинного деления (бисекции)

#### Принцип работы
Метод основан на теореме Больцано-Коши: если непрерывная функция на концах отрезка принимает значения разных знаков, то внутри отрезка существует корень.

#### Алгоритм
1. Выбираем начальный интервал [a, b], где f(a)×f(b) < 0
2. Вычисляем середину: c = (a + b) / 2
3. Если f(c) = 0 → корень найден
4. Если f(a)×f(c) < 0 → корень в [a, c], иначе в [c, b]
5. Повторяем до достижения нужной точности

#### Оценка погрешности
После n итераций: |ξ - cₙ| ≤ (b - a) / 2ⁿ⁺¹

### 2. Метод простых итераций

#### Принцип работы
Уравнение f(x) = 0 преобразуется к виду x = φ(x), затем строится итерационная последовательность.

#### Преобразование уравнения
Исходное: x² - eˣ + 2 = 0
Преобразованное: x = ln(x² + 2)

#### Условие сходимости
Метод сходится, если |φ'(x)| < 1 в окрестности корня.
В нашем случае: φ'(x) = (2x)/(x² + 2)

#### Алгоритм
1. Выбираем начальное приближение x₀
2. Вычисляем xₙ₊₁ = φ(xₙ)
3. Повторяем до |xₙ₊₁ - xₙ| < ε

### 3. Метод Ньютона (касательных)

#### Принцип работы
Использует линейную аппроксимацию функции в текущей точке.

#### Формула итерации
```
        f(xₙ)
xₙ₊₁ = xₙ - --------
        f'(xₙ)
```

где f'(x) = 2x - eˣ

#### Условия сходимости
- f(x) ∈ C²[a,b] (дважды непрерывно дифференцируема)
- f'(x) ≠ 0 на [a,b]
- f''(x) не меняет знак на [a,b]
- Начальное приближение выбирается по правилу: f(x₀)×f''(x₀) > 0

## 💻 Объяснение работы кода

### Заголовочные файлы

#### `include/functions.h`
```cpp
#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <cmath>

// Исходная функция
inline double f(double x) {
    return x*x - std::exp(x) + 2;
}

// Итерационная функция для метода простых итераций
inline double phi(double x) {
    return std::log(x*x + 2);
}

// Производные для метода Ньютона
inline double f_prime(double x) {
    return 2*x - std::exp(x);
}

#endif
```
**Назначение**: Содержит математические функции и их производные.

#### `include/methods.h`
```cpp
#ifndef METHODS_H
#define METHODS_H

// Структуры для хранения результатов
struct BisectionResult {
    int iterations;
    double root;
    double func_value;
};

// Объявления методов
BisectionResult bisection_method(double a, double b, double epsilon);
// ... другие методы

#endif
```
**Назначение**: Определяет структуры данных и объявляет функции методов.

### Исходные файлы

#### `src/bisection.cpp`
```cpp
#include "functions.h"
#include "methods.h"

BisectionResult bisection_method(double a, double b, double epsilon) {
    BisectionResult result;
    result.iterations = 0;

    while ((b - a) > 2 * epsilon) {
        double c = (a + b) / 2;

        if (f(a) * f(c) < 0) {
            b = c;
        } else {
            a = c;
        }
        result.iterations++;
    }

    result.root = (a + b) / 2;
    result.func_value = f(result.root);

    return result;
}
```
**Логика**: Реализует алгоритм бисекции, подсчитывает итерации и возвращает результат.

#### `src/iteration.cpp` и `src/newton.cpp`
Аналогично реализуют соответствующие методы с подсчетом итераций.

#### `src/main.cpp`
```cpp
#include <iostream>
#include <iomanip>
#include "methods.h"

int main() {
    // Параметры вычислений
    double a = 1.0, b = 1.5;
    double x0_iter = 1.25;
    double x0_newton = 1.5;

    // Вычисления для двух точностей
    BisectionResult bisect1 = bisection_method(a, b, 1e-6);
    // ... другие вычисления

    // Форматированный вывод таблицы
    std::cout << "==================================================================================" << std::endl;
    // ... вывод результатов
}
```
**Логика**: Координирует вычисления, форматирует и выводит результаты.

### Makefile
```makefile
CXX = g++
CXXFLAGS = -std=c++11 -Iinclude
SRCDIR = src
OBJDIR = obj

SOURCES = $(SRCDIR)/main.cpp $(SRCDIR)/bisection.cpp $(SRCDIR)/iteration.cpp $(SRCDIR)/newton.cpp
OBJECTS = $(SOURCES:$(SRCDIR)/%.cpp=$(OBJDIR)/%.o)
TARGET = lab4

$(TARGET): $(OBJECTS)
	$(CXX) $(OBJECTS) -o $(TARGET)

$(OBJDIR)/%.o: $(SRCDIR)/%.cpp
	@mkdir -p $(OBJDIR)
	$(CXX) $(CXXFLAGS) -c $< -o $@
```
**Назначение**: Автоматизирует процесс сборки проекта.

## 📊 Анализ результатов

### Полученные результаты
```
==================================================================================
СРАВНИТЕЛЬНАЯ ТАБЛИЦА МЕТОДОВ РЕШЕНИЯ УРАВНЕНИЯ x^2 - e^x + 2 = 0
==================================================================================
          Метод        Точность    Итерации         Корень          f(корень)
==================================================================================
Половинное деление       1e-6         18        1.31907367706    -2.26557e-10
Простые итерации         1e-6         30        1.31907173067     2.14433e-06
Ньютона                  1e-6          3        1.31907385442    -1.95644e-07
----------------------------------------------------------------------------------
Половинное деление       1e-11        35        1.31907367685     5.92726e-12
Простые итерации         1e-11        63        1.31907367684     2.13587e-11
Ньютона                  1e-11         4        1.31907367686    -2.75335e-14
==================================================================================
```

### Ключевые выводы

#### 1. Скорость сходимости
- **Метод Ньютона**: 3-4 итерации (квадратичная сходимость)
- **Метод половинного деления**: 18-35 итераций (линейная сходимость)
- **Метод простых итераций**: 30-63 итерации (линейная сходимость)

#### 2. Точность результатов
Все методы достигли требуемой точности:
- Найденный корень: x ≈ 1.3190736768
- Значение функции в корне близко к нулю (10⁻¹⁰ - 10⁻¹⁴)

#### 3. Эффективность методов
- **Метод Ньютона** - самый быстрый, но требует вычисления производной
- **Метод половинного деления** - самый надежный, гарантированно сходится
- **Метод простых итераций** - требует тщательного выбора итерационной функции

## ⚠️ Возможные проблемы и решения

### Проблема 1: Ошибки компиляции
**Симптом**: `cannot open source file "functions.h"`
**Решение**: Убедитесь, что:
- Файлы заголовков находятся в папке `include/`
- В Makefile указан флаг `-Iinclude`
- Структура проекта соответствует описанной

### Проблема 2: Неверные результаты
**Симптом**: Программа выдает неверные значения корня
**Решение**: Проверьте:
- Правильность математических функций в `functions.h`
- Корректность интервала изоляции корня
- Условия сходимости для методов итераций и Ньютона

### Проблема 3: Программа не компилируется
**Симптом**: Ошибки линковки или компиляции
**Решение**:
- Убедитесь, что установлен компилятор C++ (g++ или clang++)
- Проверьте синтаксис Makefile
- Выполните `make clean` и пересоберите проект

### Проблема 4: Бесконечный цикл
**Симптом**: Программа не завершается
**Решение**: Проверьте:
- Критерии остановки в методах
- Условия сходимости
- Максимальное количество итераций (в коде установлено 1000)

## 🔬 Дальнейшее развитие проекта

1. **Добавление графического интерфейса** для визуализации работы методов
2. **Реализация дополнительных методов** (хорд, секущих, комбинированных)
3. **Анализ устойчивости** методов к выбору начальных приближений
4. **Тестирование на различных функциях** для сравнения эффективности
5. **Параллельная реализация** для ускорения вычислений

## 📚 Теоретическая справка

### Теорема Больцано-Коши
Если функция f(x) непрерывна на отрезке [a,b] и f(a)×f(b) < 0, то существует точка c ∈ (a,b) такая, что f(c) = 0.

### Скорости сходимости методов
- **Метод половинного деления**: линейная, O(1/2ⁿ)
- **Метод простых итераций**: линейная, O(qⁿ) где q < 1
- **Метод Ньютона**: квадратичная, O(ε²ⁿ)

### Практические рекомендации
- Для инженерных расчетов: ε = 10⁻³ ÷ 10⁻⁶
- Для научных исследований: ε = 10⁻⁶ ÷ 10⁻¹²
- Для учебных примеров: ε = 10⁻² ÷ 10⁻⁴

---

**Автор**: [Ваше имя]
**Дата**: [Текущая дата]
**Курс**: Численные методы
**Учебное заведение**: [Название вуза]
