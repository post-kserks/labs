# Лабораторная работа 9: Алгоритмы поиска для составления учебного плана

## Описание проекта

Данный проект реализует три классических алгоритма поиска для решения задачи составления оптимального учебного плана:

- **Hill Climbing (HC)** — жадный локальный поиск
- **Beam Search** — поиск с ограниченной шириной луча
- **Simulated Annealing (SA)** — вероятностный поиск с имитацией отжига

Каждый алгоритм демонстрирует разный подход к оптимизации последовательности тем с учётом пререквизитов и новизны.

## Структура проекта

```
lab9/
├── main.cpp           # Точка входа программы, обработка аргументов
├── algorithms.cpp     # Реализация всех трёх алгоритмов
├── algorithms.h       # Заголовочный файл с прототипами функций
├── data.h             # Данные: темы, пререквизиты, канонический порядок
├── Makefile           # Сборка проекта
└── README.md          # Документация проекта
```

## Компиляция и запуск

### Требования
- C++17 или выше
- g++ компилятор
- make

### Компиляция

```bash
make
```

### Очистка

```bash
make clean
```

### Запуск

Программа поддерживает три режима работы:

```bash
./lab9 hc      # Hill Climbing
./lab9 beam    # Beam Search
./lab9 sa      # Simulated Annealing
```

## Описание алгоритмов

### 1. Hill Climbing (HC)

**Назначение**: Построение логичной последовательности тем без инноваций.

**Как работает**:
1. Начинает с канонического порядка
2. На каждой итерации пробует случайные перестановки (swap двух элементов)
3. Если находится улучшение — принимает его
4. Останавливается при достижении локального максимума

**Параметры**:
- `w_coherence = 5.0` — высокий штраф за нарушение пререквизитов
- `w_novelty = -0.1` — отрицательный вес за отклонение от стандарта
- `max_iters = 200` — максимум итераций

**Результат**: Возвращает канонический порядок или близкую к нему последовательность.

### 2. Beam Search

**Назначение**: Создание нескольких альтернативных учебных планов.

**Как работает**:
1. Начинает с пустой последовательности
2. На каждом шаге расширяет все пути, добавляя по одной теме
3. Оставляет только `β` лучших путей (ширина луча)
4. Продолжает, пока не построены полные последовательности

**Параметры**:
- `beta = 3` — количество альтернативных планов
- `w_coherence = 2.0` — умеренный штраф за нарушения
- `w_novelty = 0.5` — поощрение за новизну

**Результат**: Возвращает 3 различных учебных плана.

### 3. Simulated Annealing (SA)

**Назначение**: Поиск инновационных подходов к составлению учебного плана.

**Как работает**:
1. Начинает с начального состояния (канонического или случайного)
2. На каждой итерации генерирует случайного соседа (swap)
3. Принимает улучшения всегда, ухудшения — с вероятностью `exp(Δ/T)`
4. Температура `T` линейно уменьшается от `start_temp` до `end_temp`
5. Выполняет несколько запусков для устойчивости

**Параметры**:
- `w_coherence = 1.5` — низкий штраф за нарушения
- `w_novelty = 1.0` — сильное поощрение за новизну
- `max_iters = 2000` — большое количество итераций
- `start_temp = 1.0`, `end_temp = 0.001` — диапазон температур
- `runs = 4` — количество запусков

**Результат**: Возвращает наиболее инновационный план из 4 запусков.

## Функция оценки (Value Function)

Качество последовательности оценивается по формуле:

```
Value = -violations × w_coherence + disagreements × w_novelty
```

Где:
- **violations** — количество нарушений пререквизитов (чем меньше, тем лучше)
- **disagreements** — количество отличий от канонического порядка (мера новизны)
- **w_coherence** — вес за соблюдение пререквизитов
- **w_novelty** — вес за инновационность

## Данные

### Темы учебного плана

```cpp
0: Intro              // Введение
1: Basics             // Основы программирования
2: DataStructures     // Структуры данных
3: Algorithms         // Алгоритмы
4: Databases          // Базы данных
5: WebDev             // Веб-разработка
6: AI_Fundamentals    // Основы ИИ
7: Project            // Финальный проект
```

### Пререквизиты

- `Basics` → `DataStructures`
- `Basics` → `Algorithms`
- `DataStructures` → `Algorithms`

### Канонический порядок

```
Intro → Basics → DataStructures → Algorithms → Databases → WebDev → AI_Fundamentals → Project
```

## Примеры вывода

### Hill Climbing

```
[HC] Hill Climbing — строит логичную последовательность без инноваций
Результат HC:
Intro -> Basics -> DataStructures -> Algorithms -> Databases -> WebDev -> AI_Fundamentals -> Project
Value: -0
Время (ms): 0
```

### Beam Search

```
[Beam] Beam Search — создаёт несколько альтернативных учебных планов
Найдено 3 план(а/ов):
1) Project -> AI_Fundamentals -> WebDev -> Databases -> Basics -> DataStructures -> Algorithms -> Intro
   Value: 12.5
2) Project -> AI_Fundamentals -> WebDev -> Databases -> Basics -> DataStructures -> Intro -> Algorithms
   Value: 12
3) Project -> AI_Fundamentals -> Databases -> WebDev -> Basics -> DataStructures -> Algorithms -> Intro
   Value: 12
Время (ms): 0
```

### Simulated Annealing

```
[SA] Simulated Annealing — ищет инновационные подходы
Run 1: Project -> AI_Fundamentals -> WebDev -> Databases -> Basics -> DataStructures -> Algorithms -> Intro   Value=25
Run 2: Project -> AI_Fundamentals -> WebDev -> Databases -> Basics -> DataStructures -> Algorithms -> Intro   Value=25
Run 3: Project -> AI_Fundamentals -> WebDev -> Databases -> Basics -> DataStructures -> Algorithms -> Intro   Value=25
Run 4: Project -> AI_Fundamentals -> WebDev -> Databases -> Basics -> DataStructures -> Algorithms -> Intro   Value=25

Лучший найденный SA-план:
Project -> AI_Fundamentals -> WebDev -> Databases -> Basics -> DataStructures -> Algorithms -> Intro
Value: 25
```

## Особенности реализации

### Без ООП
Проект реализован в процедурном стиле без использования объектно-ориентированного программирования.

### Комментарии на русском
Все комментарии в коде написаны на русском языке для лучшего понимания.

### Простота
Код максимально простой и понятный, без излишних абстракций.

### Детерминизм
Hill Climbing и Beam Search дают стабильные результаты. Simulated Annealing использует случайность, поэтому результаты могут варьироваться.

## Интерпретация результатов

### HC vs Beam vs SA

| Алгоритм | Соблюдение пререквизитов | Новизна | Скорость |
|----------|-------------------------|---------|----------|
| **HC** | ✅ Высокое | ❌ Низкая | ⚡ Быстро |
| **Beam** | ⚠️ Среднее | ✅ Средняя | ⚡ Быстро |
| **SA** | ❌ Низкое | ✅✅ Высокая | 🐌 Медленно |

### Когда использовать каждый алгоритм

- **Hill Climbing**: Когда нужен быстрый и безопасный результат
- **Beam Search**: Когда нужны альтернативные варианты с балансом между традиционностью и новизной
- **Simulated Annealing**: Когда нужны творческие и инновационные решения

## Настройка параметров

Параметры алгоритмов можно настроить в [`main.cpp`](main.cpp):

```cpp
// Hill Climbing
double w_coh = 5.0;   // вес за coherence
double w_nov = -0.1;  // вес за novelty
int max_iters = 200;  // итерации

// Beam Search
int beta = 3;         // ширина луча
double w_coh = 2.0;
double w_nov = 0.5;

// Simulated Annealing
double w_coh = 1.5;
double w_nov = 1.0;
int max_iters = 2000;
double start_temp = 1.0;
double end_temp = 0.001;
int runs = 4;         // количество запусков
```

## Автор

Лабораторная работа 9: Алгоритмы поиска для составления учебного плана

## Лицензия

Учебный проект
